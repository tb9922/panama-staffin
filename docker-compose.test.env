# Intentionally empty.
# Prevent docker compose test runs from reading the application .env file,
# where bcrypt/JWT-style values can contain $ characters that Compose tries
# to interpolate before starting the disposable test database.
